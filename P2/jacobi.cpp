#include "imath.h"
#include "integer.h"

integer J(integer& x, integer& y{
	if(x == 1){
		return 1;
	}
	//else if(x is even){
		//return J(x/2,y)*((-1)^(((x-1)*(y-1))/4))
	//}

	//else
	//return J( y(mod x), x)*((-1)^(((x-1)*(y-1))/4))
}
